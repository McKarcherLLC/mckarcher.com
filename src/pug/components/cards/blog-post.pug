mixin blog-post(img, title, desc)
  .card.post
    figure
      |
      |
      img(src=img alt='thumb')/
      |
      |
    .text
      h5.use-text-subtitle2 #{title}
      p.use-text-paragraph #{desc}
    |
    |
    a.btn-flat.primary.readmore.waves-effect #{$t.femine_landing.read_more}
    |
    |
